export function checkMaybeCreateRoom(uid: string) {
  // Check if a room exists where length === 2 && room.members.includes(uid) and if not, create one and in both cases redirect the user to that one.
  console.log("Check or create a room function");
}
